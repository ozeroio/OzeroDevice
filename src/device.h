#ifndef OZEROIO_DEVICE_H
#define OZEROIO_DEVICE_H

#ifndef ozero_max
#define ozero_max(x, y) (((x) > (y)) ? (x) : (y))
#endif

#ifndef ozero_min
#define ozero_min(x, y) (((x) < (y)) ? (x) : (y))
#endif

#endif // OZEROIO_DEVICE_H
